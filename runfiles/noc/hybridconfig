topology = mesh;
k = 8;
n = 2;

sim_type = throughput;

packet_size = 1;
warmup_periods = 1;

sample_period = 400000;
max_samples = 5;

burst_alpha = 0.5;
burst_beta = -1.0;

// power
sim_power = 1;
tech_file = techfile.txt;

// When simulate in the sync mode, channel_width is used to estimate performance
channel_width = 256;

// Routing
routing_function = xy_yx;


// Flow control
num_vcs = 2;
vc_buf_size = 2;

// Traffic
traffic = focus;
injection_process = focus;

